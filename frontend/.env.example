# Backend API URL. Leave unset to use built-in mock data (for preview/development).
VITE_API_URL=https://qazo-tracker.vercel.app

# Set to "true" to force mock data even when VITE_API_URL is set.
VITE_USE_MOCK=false
